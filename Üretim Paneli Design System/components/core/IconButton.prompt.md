One-line: circular icon-only control for top-bar utilities and card overflow menus.

```jsx
<IconButton icon="sun" label="Tema değiştir" />
<IconButton icon="ellipsis-vertical" label="Daha fazla" variant="ghost" />
```

Always circular (pill radius), always carries a Turkish `label`. `soft` in the top bar, `ghost` inside cards.
