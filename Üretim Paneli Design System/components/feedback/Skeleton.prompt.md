One-line: shimmer placeholder shown while the Excel source is being re-read.

```jsx
<Skeleton height={30} width={120} />
<Skeleton lines={3} />
```

Match the skeleton's height to the real element (30px for a KPI value, 260px for a plot).
