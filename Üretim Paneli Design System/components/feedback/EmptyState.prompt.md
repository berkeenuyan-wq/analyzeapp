One-line: centred placeholder inside a card when a filter returns nothing.

```jsx
<EmptyState icon="calendar-x" title="Bu tarihte kayıt yok"
  description="Seçilen aralıkta pres kaydı bulunamadı. Tarihi değiştirin veya veri girin."
  action={<Button variant="primary" icon="square-pen">Veri Girişi</Button>} />
```
