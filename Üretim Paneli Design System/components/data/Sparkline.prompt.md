One-line: trend shape without axes — for KPI tiles, table rows and the HeroMetric aside.

```jsx
<Sparkline points={[812, 840, 798, 866, 903, 889, 921]} width={180} height={48} />
```

No labels, no gridlines. If the reader needs to read values, use a ChartCard instead.
