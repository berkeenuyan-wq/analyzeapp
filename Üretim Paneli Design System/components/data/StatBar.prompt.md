One-line: a thin inline proportion bar with a trailing figure, for table cells and compact rankings.

```jsx
<StatBar value={412} max={1284} valueLabel="412 ton" color="var(--series-2)" />
```

Bar colour is a series token, not a signal colour — a proportion is not a verdict.
