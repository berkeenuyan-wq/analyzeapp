One-line: native select in the system's field shell — for press, shift, supplier and date-range pickers.

```jsx
<Select value={pres} onChange={setPres} options={["Pres 1","Pres 2"]} icon="factory" />
<Select value={r} onChange={setR} options={[{value:"7g",label:"Son 7 gün"},{value:"30g",label:"Son 30 gün"}]} size="sm" />
```
