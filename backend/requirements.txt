# Runtime dependencies — exact pins (docs/CODING_STANDARDS.md §5).
# Backend runs on CPython 3.12 (Homebrew python@3.12). numpy is pinned below
# pandas' upper bound to avoid an ABI mismatch in pandas' datetime kernels.
fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.9.2
pandas==2.2.3
numpy==2.2.6
openpyxl==3.1.5
